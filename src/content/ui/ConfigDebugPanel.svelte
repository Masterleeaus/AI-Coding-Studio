<!--
  Retired in the Agent 1 runtime trust-boundary repair.

  A component mounted into a third-party host page is not an extension-owned
  privileged surface. The former panel could mutate remote configuration and
  was reachable through page-visible DOM and CustomEvents. Keep this inert
  tombstone until repository cleanup can remove the file through an approved
  deletion workflow.
-->
