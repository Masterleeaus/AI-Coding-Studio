name: Android Verification

on:
  pull_request:
    branches:
      - main
      - integration/local-first-repair
    paths:
      - android/**
      - src/android/**
      - src/platform/**
      - build.js
      - package.json
      - package-lock.json
      - playwright.android.config.js
      - .github/workflows/android-verify.yml
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: android-verify-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  android:
    name: Android build and tests
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17
          cache: gradle

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install JavaScript dependencies
        run: npm ci

      - name: Install Playwright Chromium
        run: npx playwright install --with-deps chromium

      - name: Build Android JavaScript target
        run: npm run build:android

      - name: Assemble debug APK
        run: npm run android:assemble:debug

      - name: Run Android simulator parity tests
        run: npm run test:e2e:android

      - name: Run Android Gradle tests
        run: npm run android:test

      - name: Upload Android artefacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: android-verification-artefacts
          path: |
            android/app/build/outputs/apk/debug/
            android/app/build/reports/
            playwright-report-android/
          if-no-files-found: ignore
